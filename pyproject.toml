[project]
name = "comfyui_mlsuite"
description = "MLSuite: a growing collection of ComfyUI nodes. Starts with Image Loader — bring any image into your workflow from an upload, a URL, a built-in gallery browser, or automatic folder rotation."
version = "0.1.1"
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = [
    "Pillow",
]

[project.urls]
Repository = "https://github.com/marcolopezs/ComfyUI_MLSuite"

[tool.comfy]
PublisherId = "marcolopezs"
DisplayName = "MLSuite"
Icon = "https://raw.githubusercontent.com/marcolopezs/ComfyUI_MLSuite/main/assets/icon.png"
